; PlatformIO Project Configuration File
;
; alpha03 - the beta1 codebase. DRV8833 + AS5600 closed-loop wheel position,
; runtime-selectable motor direction, RS485 transport on USART0 with RE/DE
; direction control, debug port on USART1, a dedicated second SK6812
; (LED2, replacing the old ISP-header-sharing ext LED) and AT24CS02 I2C
; EEPROM/serial-number support - ahead of the beta1 schematic that adds
; the LED2 pin and the AT24CS02, neither of which exist on the V0.2a/b
; boards this otherwise still targets. See project.md.
;
; Board runs on its internal 8MHz RC oscillator (no crystal populated) and
; is flashed via ISP (no bootloader) - same as TestBench04.

[platformio]
default_envs = atmega328pb_isp

[env:atmega328pb_isp]
platform = atmelavr
board = ATmega328PB
framework = arduino

; Internal 8MHz RC oscillator - no crystal on this board.
board_build.f_cpu = 8000000L
board_hardware.oscillator = internal
board_hardware.uart = uart0
board_hardware.bod = 2v7
board_hardware.eesave = yes

; USBasp programmer via ISP (6-pin header into the AVR's SPI/reset pins,
; the same physical header USART1/Serial1 debug output is wired through -
; see pins_config.h). No bootloader involved: `pio run -t upload` writes
; flash directly. Swap upload_protocol below if using a different
; programmer:
;   upload_protocol = stk500v1      ; classic Arduino-as-ISP
;   upload_protocol = avrispmkii    ; Atmel/Microchip AVRISP mkII
upload_protocol = usbasp

upload_flags = -B 8

monitor_speed = 9600

lib_deps = adafruit/Adafruit NeoPixel@^1.12.5
